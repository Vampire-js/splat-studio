fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.12
pydantic==2.9.2

# Real pipeline deps (optional at runtime — pipeline falls back to the stub if
# missing). pycolmap bundles a COLMAP build so no system install is needed.
pycolmap==4.0.4
Pillow==10.4.0
numpy==1.26.4
